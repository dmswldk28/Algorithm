-- 코드를 작성해주세요
SELECT
    SUM(H.SCORE) AS SCORE,
    E.EMP_NO,
    E.EMP_NAME,
    E.POSITION,
    E.EMAIL
FROM
    HR_EMPLOYEES E
JOIN
    HR_GRADE H ON H.EMP_NO = E.EMP_NO
WHERE
    YEAR = 2022
GROUP BY
    E.EMP_NO
ORDER BY
    SCORE DESC
LIMIT
    1;